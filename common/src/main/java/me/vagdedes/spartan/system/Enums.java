package me.vagdedes.spartan.system;

public class Enums {

    public enum HackType {
        KillAura, HitReach, Speed, Flight, NoFall, Timer, FastClicks,
        AutoClicker, Velocity, NoSlowdown, FastBreak, FastPlace,
        Scaffold, Phase, Jesus, Step, ElytraMove, BoatMove,
        ImpossibleActions, Exploits, XRay, GhostHand, FastEat,
        FastHeal, FastBow, Criticals, AimBot, NoSwing,
        MorePackets, BlockReach, IrregularMovements
    }

    public enum Permission {
        STAFF, ADMIN, BYPASS
    }

    public enum ToggleAction {
        ENABLE, DISABLE
    }
}
